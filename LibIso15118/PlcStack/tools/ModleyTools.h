#ifndef MODLEYTOOLS_H
#define MODLEYTOOLS_H

#include <ctype.h>

unsigned char todigit (const unsigned char c);
unsigned int hexencode (void * memory, unsigned int extent, char const * string);

#endif
